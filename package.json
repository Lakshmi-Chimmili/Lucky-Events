{
  "name": "luckyevents",
  "version": "1.0.0",
  "description": "Full-stack MERN Event Management Booking Platform with ExpressJS, MongoDB, React, and Tailwind CSS.",
  "main": "backend/src/server.js",
  "scripts": {
    "start": "node backend/src/server.js",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "seed": "npm run seed --prefix backend",
    "install-deps": "npm install --prefix backend && npm install --prefix frontend --include=dev",
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^9.2.4"
  },
  "keywords": [
    "event-management",
    "mern",
    "express",
    "react",
    "mongodb",
    "tailwind",
    "booking"
  ],
  "author": "",
  "license": "ISC"
}
